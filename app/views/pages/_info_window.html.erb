<% user_selected = User.find(user) %>
<div class="p-3">
  <p>email: <%= user_selected.email %></p>
  <p><%= user_selected.address %></p>
  <p>favorite meal: <%= user_selected.meal&.name %></p>
  <%= link_to 'open', write_modal_path(user_selected), class: "btn btn-primary", method: :post, remote: true %>
</div>
